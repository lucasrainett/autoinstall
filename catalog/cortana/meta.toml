name = "Cortana"
description = "Microsoft's retired voice assistant, still present on older Windows builds"
kind = "install"
website = "https://support.microsoft.com/en-us/cortana/end-of-support-for-cortana"

capabilities = ["voice-assistant"]

[windows]
installMethod = "script"
notes = "Preinstalled, so it usually starts already selected. Unchecking it removes the package for the current user; it can be reinstalled from the Microsoft Store."
