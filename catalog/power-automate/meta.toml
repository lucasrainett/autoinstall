name = "Power Automate"
description = "Microsoft's desktop automation tool, preinstalled on Windows 11"
kind = "install"
website = "https://www.microsoft.com/en-us/power-platform/products/power-automate"

capabilities = ["desktop-automation"]

[windows]
installMethod = "script"
notes = "Preinstalled, so it usually starts already selected. Unchecking it removes the package for the current user; it can be reinstalled from the Microsoft Store."
