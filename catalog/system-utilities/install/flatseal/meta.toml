name = "Flatseal"
description = "Reviews and edits the permissions Flatpak applications hold"
website = "https://github.com/tchx84/Flatseal"

[linux]
installMethod = "flatpak"
notes = "Linux-only by design: it manages Flatpak/AppImage/GNOME internals that exist only on Linux."
