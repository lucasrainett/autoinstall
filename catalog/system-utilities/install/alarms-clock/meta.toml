name = "Alarms & Clock"
description = "Windows alarms, timers, stopwatch, world clock and focus sessions"
website = "https://apps.microsoft.com/detail/9wzdncrfj3pr"

capabilities = ["clock-timers", "task-management"]

[windows]
installMethod = "script"
notes = "Preinstalled, so it usually starts already selected. Unchecking it removes the package for the current user; it can be reinstalled from the Microsoft Store."
