name = "Video4Linux utilities"
description = "Command-line tools for inspecting and configuring webcams"
website = "https://linuxtv.org/"

[linux]
installMethod = "apt"
notes = "Linux only: Video4Linux is a kernel subsystem with no counterpart elsewhere."
requiresElevation = true
