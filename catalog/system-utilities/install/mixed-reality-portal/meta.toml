name = "Mixed Reality Portal"
description = "Windows Mixed Reality headset setup app, preinstalled on Windows"
website = "https://www.microsoft.com/windows/windows-mixed-reality"

[windows]
installMethod = "script"
notes = "Preinstalled, so it usually starts already selected. Unchecking it removes the package for the current user only; it can be reinstalled from the Microsoft Store. Microsoft retired Windows Mixed Reality in 2024, so on current builds this is dead weight."
