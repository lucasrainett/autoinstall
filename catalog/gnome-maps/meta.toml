name = "GNOME Maps"
description = "Map application that ships preinstalled on Ubuntu-family desktops"
kind = "install"
website = "https://apps.gnome.org/Maps/"

capabilities = ["maps"]

[linux]
installMethod = "apt"
notes = "Preinstalled on most Ubuntu-family desktops, so it usually starts already selected. Unchecking it purges the package."
requiresElevation = true
