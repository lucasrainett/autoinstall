name = "Signal (deb)"
description = "Private messenger, from Signal's own apt repository"
website = "https://signal.org/"

[linux]
installMethod = "apt"
notes = "Signal's own apt repository — the channel signal.org documents. Preferred over Flathub, which is community-maintained rather than published by Signal."
requiresElevation = true
