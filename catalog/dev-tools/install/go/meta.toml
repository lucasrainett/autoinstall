name = "Go"
description = "Programming language and toolchain by Google, for building fast, statically typed, compiled programs"
website = "https://go.dev"

[linux]
installMethod = "direct-download"
notes = "Downloads the latest release tarball and extracts it to /usr/local/go, adding it to PATH via ~/.bashrc."
requiresElevation = true

[macos]
installMethod = "homebrew"

[windows]
installMethod = "winget"
