# Shell aliases installed by autoinstall (quality-of-life/configure/dotfiles).
# This file is sourced from your shell rc; edit it freely — it is never regenerated in place,
# and removing the entry deletes only this file and the single line that sources it.

alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias ..='cd ..'
alias ...='cd ../..'

alias gs='git status'
alias gp='git pull'
alias gd='git diff'
alias gc='git commit'
alias gco='git checkout'

alias dc='docker compose'
alias dps='docker ps'
