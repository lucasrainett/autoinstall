name = "DistroShelf"
description = "Manages Distrobox containers from a desktop interface"
kind = "install"
website = "https://github.com/ranfdev/DistroShelf"

capabilities = ["containers", "container-gui"]

[linux]
installMethod = "flatpak"
notes = "Linux-only by design: it manages Flatpak/AppImage/GNOME internals that exist only on Linux."
