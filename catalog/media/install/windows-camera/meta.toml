name = "Camera"
description = "The Windows camera app: photos, video, and QR, document and whiteboard scanning"
website = "https://support.microsoft.com/en-us/windows/hardware/camera/how-to-use-the-windows-camera-app"

capabilities = ["webcam", "document-scanning"]

[windows]
installMethod = "script"
notes = "Preinstalled, so it usually starts already selected. Unchecking it removes the package for the current user; it can be reinstalled from the Microsoft Store."
