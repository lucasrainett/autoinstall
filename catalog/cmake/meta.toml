name = "CMake"
description = "Cross-platform build system generator"
kind = "install"
website = "https://cmake.org"

capabilities = ["build-tools"]

[linux]
installMethod = "apt"
requiresElevation = true

[macos]
installMethod = "homebrew"

[windows]
installMethod = "winget"
