name = "Sound Recorder"
description = "The Windows voice recorder"
kind = "install"
website = "https://support.microsoft.com/en-us/windows/apps/sound-recorder-app-for-windows-faq"

capabilities = ["audio-recording"]

[windows]
installMethod = "script"
notes = "Preinstalled, so it usually starts already selected. Unchecking it removes the package for the current user; it can be reinstalled from the Microsoft Store."
