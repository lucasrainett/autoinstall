name = "Infrastructure"
description = "Cloud and infrastructure-as-code tooling, containers, and virtual machines"
entries = [
  "aws-cli",
  "terraform",
  "docker",
  "podman-desktop",
  # One VM manager per platform — GNOME Boxes, UTM and VirtualBox are different programs, so
  # they are different entries. Whichever applies to the machine is the one that acts.
  "gnome-boxes",
  "utm",
  "virtualbox",
  "go",
]
